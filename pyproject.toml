[project]
name = "py-dns-changer"
version = "0.1.0"
description = "Switches DNS servers based on your need and configuration."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pillow>=12.2.0",
    "psutil>=7.2.2",
    "pyinstaller>=6.21.0",
    "pystray>=0.19.5",
]
